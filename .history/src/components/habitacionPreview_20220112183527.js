import React from 'react';

const HabitacionPreview = ({habitacion}) => {
  
  const {contenido, imagen, }habitacion 
  
  return ( 
    <div>

    </div>
    
  );
}

export default HabitacionPreview;